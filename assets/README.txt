Logo (affiché en haut de chaque page) :
  logo.png

Fond parchemin (optionnel), l'un de ces noms :
  bioguide-bg.png
  bioguide-bg.jpg  (ou .jpeg)

Le fond est intégré en base64 dans le CSS (compatible Streamlit).
Sans parchemin, un fond vert doux + effet CSS est utilisé.
